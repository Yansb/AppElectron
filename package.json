{
  "name": "PrimeiroAppElectron",
  "version": "1.0.0",
  "description": "Projeto para fazer um primeiro App com electron",

  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "lint": "tslint -c tslint.json -p tsconfig.json",
    "start": "tsc && electron ./dist/main.js"
  },
  "devDependencies": {
    "electron": "^9.4.0",
    "tslint": "^5.19.0",
    "typescript": "^3.6.0"
  }
}
